<?xml version="1.0"?>
<package format="3">
  <name>burf_platform_driver</name>
  <version>1.0.0</version>
  <description>ROS2 driver for BurfPlatform WebSocket/WebRTC server</description>
  <maintainer email="user@example.com">BurfPlatform</maintainer>
  <license>MIT</license>

  <buildtool_depend>ament_python</buildtool_depend>

  <depend>rclpy</depend>
  <depend>sensor_msgs</depend>
  <depend>geometry_msgs</depend>
  <depend>nav_msgs</depend>
  <depend>std_msgs</depend>
  <depend>cv_bridge</depend>
  <depend>tf2_ros</depend>
  <!-- Pillow is used to PNG-encode the OccupancyGrid before the driver
       relays it as a map_update message over the WebSocket. Imported
       lazily so the rest of the driver still loads if it isn't present. -->
  <exec_depend>python3-pil</exec_depend>

  <test_depend>ament_copyright</test_depend>
  <test_depend>ament_flake8</test_depend>
  <test_depend>ament_pep257</test_depend>
  <test_depend>python3-pytest</test_depend>

  <export>
    <build_type>ament_python</build_type>
  </export>
</package>
